# Leave empty for same-origin deployments, or point to the shared Spring Boot backend.
VITE_API_BASE_URL=https://your-shared-backend.example.com

# Defaults to /evaluation/latest.json. Set this only when reports are hosted elsewhere.
VITE_EVALUATION_REPORT_URL=/evaluation/latest.json
